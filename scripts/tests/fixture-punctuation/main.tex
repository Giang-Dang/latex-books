% A second fixture, for the character check alone. fixture-book covers the
% prose checks under Ascii mode; this one covers Punctuation mode, which is the
% default and the only mode under which a book in a language with diacritics
% can exist.
%
% Its check-chapter.psd1 switches every other check off, so anything this
% fixture reports is a character finding and nothing else. It is never compiled.
\documentclass[11pt,a4paper]{book}
\begin{document}
\end{document}
