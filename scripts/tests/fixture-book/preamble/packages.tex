% Exercises the \newminted discovery path: check-chapter.ps1 scrapes this
% folder to learn which environments hold verbatim text, so a fixture that
% declares one proves the scrape still works.
\usepackage{minted}
\newminted[fixturesdl]{ruby}{}
