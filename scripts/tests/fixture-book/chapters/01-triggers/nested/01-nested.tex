\section{A section in a subfolder}
\label{sec:ch01-nested}

Nothing here reaches the gate unless the chapter walk recurses, so the -- run on
this line is what proves it did. The full path has always recursed; the
-Chapter mode did not, and a drafting session runs -Chapter.
