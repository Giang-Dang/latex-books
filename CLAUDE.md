<!--
The instructions live in AGENTS.md so that every agent runtime reads one file.
Claude Code does not read AGENTS.md itself, but it expands the import below at
session start, so this stub is all that is needed. Never copy content here:
one file, one copy. This comment is stripped before the import is loaded.
-->
@AGENTS.md
