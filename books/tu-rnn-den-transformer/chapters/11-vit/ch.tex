% Scope (SPEC table of contents): Dosovitskiy và cộng sự 2021. Patch embedding, token CLS, positional embedding học được; kết quả then chốt là thua ResNet ở ImageNet-1k và thắng khi pretrain JFT-300M; ViT nhỏ tự train trên CIFAR-10 để tái hiện đúng chỗ nó thua; gap: đói dữ liệu, bậc hai với ảnh lớn.
\chapter{Ảnh là 16x16 chữ}
\label{ch:vit}
