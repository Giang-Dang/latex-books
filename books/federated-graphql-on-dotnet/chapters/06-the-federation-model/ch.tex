\chapter{The Federation Model}
\label{ch:the-federation-model}

\emph{Not yet drafted.} Planned scope: Supergraph and subgraphs; entity ownership; the Apollo Federation v2 directive tour (\code{@key}, \code{@shareable}, \code{@external}, \code{@requires}, \code{@provides}, \code{@override}, \code{@interfaceObject}); composition rules conceptually.

% Drafting adds one file per section here, paths from the book root, e.g.:
% \input{chapters/06-the-federation-model/01-section-name}
