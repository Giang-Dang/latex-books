% Book-specific commands, environments and metadata.

% Title metadata used by the frontmatter pages.
\newcommand{\thebooktitle}{Federated GraphQL on .NET}
\newcommand{\thebooksubtitle}{Building and running distributed graphs with HotChocolate and WunderGraph Cosmo}
\newcommand{\thebookauthor}{Giang Dang}

% Inline code.
\newcommand{\code}[1]{\texttt{#1}}
