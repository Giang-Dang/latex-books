% Book-specific commands, environments and metadata.

% Title metadata used by the frontmatter pages - edit for each book.
\newcommand{\thebooktitle}{Book Title}
\newcommand{\thebooksubtitle}{An Optional Subtitle}
\newcommand{\thebookauthor}{Author Name}

% Example helper macro - replace with the book's own.
\newcommand{\code}[1]{\texttt{#1}}

% Theorem environments (amsthm) - uncomment if the book needs them.
% \theoremstyle{definition}
% \newtheorem{definition}{Definition}[chapter]
% \theoremstyle{plain}
% \newtheorem{theorem}{Theorem}[chapter]
